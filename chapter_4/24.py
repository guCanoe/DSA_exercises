def str2num(d, s):
    r = 0
    for i in range(len(s)):
        r+=d[s[-(i+1)]]*10**i
    return r
    
def sum_puzzle(a, b, c)
    letters = set(list(a) + list(b) + list(c))

def reange(unseted, seted):
    for i in len(letter):
        for 
